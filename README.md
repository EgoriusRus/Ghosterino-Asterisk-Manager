# Asterisk Manager - Генератор конфигураций для Asterisk

Система управления и генерации конфигурационных файлов для IP-телефонии на базе Asterisk PBX.

## Возможности

- 📊 Парсинг CSV файлов с данными о сотрудниках и телефонах
- 🔧 Генерация конфигураций для IP-телефонов (Yealink T27G, T23G, Fanvil)
- 📞 Создание SIP-конфигов пользователей
- 🎯 Формирование диалпланов Asterisk
- 🌐 Настройка Cisco маршрутизации
- 💾 Интеграция с PostgreSQL через GORM

## Технологии

- **Backend:** Go 1.24
- **Database:** PostgreSQL 16
- **ORM:** GORM
- **Deployment:** Docker, Docker Compose

## Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Go 1.24+ (для локальной разработки)

### Установка и запуск

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/EgoriusRus/Ghosterino-Asterisk-Manager.git
cd Ghosterino-Asterisk-Manager
```

2. **Настройте переменные окружения:**
```bash
cp .env.example .env
# Отредактируйте .env при необходимости
```

3. **Запустите через Docker Compose:**
```bash
# Запуск PostgreSQL и приложения
docker compose up -d

# Просмотр логов
docker compose logs -f

# Остановка
docker compose down
```

### Локальная разработка

1. **Установите зависимости:**
```bash
cd backend
go mod download
```

2. **Запустите PostgreSQL:**
```bash
docker compose up -d postgres
```

3. **Запустите приложение:**
```bash
go run main.go
```

## Структура проекта

```
.
├── backend/
│   ├── main.go                  # Точка входа
│   ├── services/
│   │   └── asterisk.go          # Логика генерации конфигов
│   ├── repositories/
│   │   ├── database.go          # Инициализация БД и GORM
│   │   └── database_test.go     # Тесты подключения
│   ├── results/                 # Сгенерированные конфиги
│   ├── Dockerfile               # Docker образ приложения
│   └── go.mod                   # Go зависимости
├── docker-compose.yml           # Оркестрация контейнеров
├── .env.example                 # Шаблон переменных окружения
├── .gitignore                   # Игнорируемые файлы
└── README.md                    # Документация
```

## Использование

### Генерация конфигураций из CSV

Поместите файл `test.csv` в корень проекта и запустите:

```bash
docker compose up backend
```

Результаты будут сохранены в `backend/results/`:
- `tftpboot/` - конфиги телефонов по MAC-адресам
- `UsersConf/` - SIP конфигурации пользователей
- `ExtConf/` - файлы диалплана Asterisk
- `CiscoConf.txt` - конфигурация Cisco

### Формат CSV

CSV файл должен содержать следующие колонки:
- ФИО
- Должность
- № телефона городской
- № телефона внутренний (4 цифры)
- Группа pickup
- Ring группа
- Тип телефона (T27G, T23G, Fanvil)
- MAC адрес
- VLAN настройки
- IP сервера
- Email
- И другие параметры...

## Переменные окружения

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `DB_HOST` | Хост PostgreSQL | `localhost` |
| `DB_PORT` | Порт PostgreSQL | `5432` |
| `DB_USER` | Пользователь БД | `postgres` |
| `DB_PASSWORD` | Пароль БД | `postgres` |
| `DB_NAME` | Имя базы данных | `asterisk_manager` |
| `APP_PORT` | Порт приложения | `8080` |

## База данных

### Подключение к PostgreSQL

```go
import "romchek-asteriska/repositories"

// Из переменных окружения
repos := repositories.InitRepos(repositories.ConnectionStringFromEnv())

// Или вручную
connStr := repositories.ConnectionString("localhost", "5432", "user", "pass", "dbname")
repos := repositories.InitRepos(connStr)
```

### Миграции

```go
repos.MigrateDB()
```

## Docker

### Сборка образа

```bash
docker compose build backend
```

### Управление контейнерами

```bash
# Запуск только PostgreSQL
docker compose up -d postgres

# Перезапуск backend
docker compose restart backend

# Просмотр статуса
docker compose ps

# Удаление volumes
docker compose down -v
```

## Разработка

### Тестирование

```bash
cd backend
go test ./...
```

### Линтинг

```bash
go vet ./...
```

### Сборка бинарника

```bash
cd backend
go build -o asterisk-manager .
```

## Статистика генерации

После запуска программа выводит статистику:
- Всего записей обработано
- Активных устройств
- Количество телефонов по типам (T27G, T23G, Fanvil)
- Устройств с MAC адресами
- Локальных номеров

## Лицензия

MIT

## Автор

EgoriusRus - [GitHub](https://github.com/EgoriusRus)

## Поддержка

При возникновении проблем создайте [issue](https://github.com/EgoriusRus/Ghosterino-Asterisk-Manager/issues) в репозитории.
