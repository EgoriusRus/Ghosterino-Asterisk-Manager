# Asterisk Manager - Генератор конфигураций для Asterisk

Система управления и генерации конфигурационных файлов для IP-телефонии на базе Asterisk PBX с REST API и веб-интерфейсом управления.

## Возможности

- 🌐 **REST API** для управления профилями, устройствами и локациями
- 📊 Загрузка данных из PostgreSQL базы данных
- 🔧 Генерация конфигураций для IP-телефонов (Yealink T27G, T23G, Fanvil, Cisco)
- 📞 Создание SIP-конфигов пользователей
- 🎯 Формирование диалпланов Asterisk
- 🌐 Настройка Cisco маршрутизации
- 💾 Полная интеграция с PostgreSQL через GORM
- 🌱 Database seeder для тестовых данных

## Технологии

- **Backend:** Go 1.24
- **Web Framework:** Fiber v2
- **Database:** PostgreSQL 16 (с network типами: inet, cidr, macaddr)
- **ORM:** GORM
- **Deployment:** Docker, Docker Compose

## Архитектура

Проект следует чистой архитектуре с разделением на слои:

```
backend/
├── cmd/
│   ├── generator/    # Генератор конфигов Asterisk
│   └── seed/         # Заполнение БД тестовыми данными
├── domain/           # Модели данных (Profile, Device, Location)
├── repositories/     # Слой доступа к данным (без GetDB)
├── services/         # Бизнес-логика (генератор конфигов)
├── handlers/         # HTTP handlers для REST API
├── main.go           # Fiber REST API сервер
└── routes.go         # Маршруты API
```

## Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Go 1.24+ (для локальной разработки)
- Make (опционально)

### Установка и запуск

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/EgoriusRus/Ghosterino-Asterisk-Manager.git
cd Ghosterino-Asterisk-Manager
```

2. **Запустите через Docker Compose:**
```bash
# Запуск PostgreSQL и API сервера
docker compose up -d

# Просмотр логов
docker compose logs -f

# Остановка
docker compose down
```

3. **Заполните базу тестовыми данными:**
```bash
cd backend
make seed
```

4. **Сгенерируйте конфиги Asterisk:**
```bash
make generator
```

### API доступен по адресу

```
http://localhost:8080
```

## Makefile команды

```bash
make help          # Показать все доступные команды
make build         # Собрать API сервер
make run           # Запустить API сервер
make seed          # Заполнить базу тестовыми данными
make generator     # Запустить генератор конфигов
make clean         # Очистить собранные файлы
make docker-up     # Поднять Docker Compose
make docker-down   # Остановить Docker Compose
make docker-logs   # Показать логи Docker
make test          # Запустить тесты
```

## REST API Endpoints

### Профили (Сотрудники)
- `GET /api/profiles` - Список всех профилей с данными локаций (JOIN)
- `GET /api/profiles/:id` - Один профиль по ID
- `POST /api/profiles` - Создать новый профиль
- `PUT /api/profiles/:id` - Обновить профиль
- `DELETE /api/profiles/:id` - Удалить профиль

### Устройства
- `GET /api/devices` - Список всех устройств
- `GET /api/devices/:mac` - Одно устройство по MAC
- `POST /api/devices` - Создать устройство
- `PUT /api/devices/:mac` - Обновить устройство
- `DELETE /api/devices/:mac` - Удалить устройство

### Локации
- `GET /api/locations` - Список всех локаций
- `GET /api/locations/:id` - Одна локация по ID
- `POST /api/locations` - Создать локацию
- `PUT /api/locations/:id` - Обновить локацию
- `DELETE /api/locations/:id` - Удалить локацию

### Примеры использования API

**Получить все профили:**
```bash
curl http://localhost:8080/api/profiles
```

**Создать новый профиль:**
```bash
curl -X POST http://localhost:8080/api/profiles \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Иванов Иван Иванович",
    "email": "ivanov@example.com",
    "device": "80:5e:c0:11:22:33",
    "locationId": 1,
    "internalNumber": 1234,
    "externalNumber": "123456",
    "ringGroup": 5000,
    "pickupGroup": 10,
    "isActive": true
  }'
```

**Обновить профиль:**
```bash
curl -X PUT http://localhost:8080/api/profiles/1 \
  -H "Content-Type: application/json" \
  -d '{"name": "Новое Имя", "email": "new@example.com"}'
```

**Удалить профиль:**
```bash
curl -X DELETE http://localhost:8080/api/profiles/1
```

## База данных

### Схема: sipadmin

**Таблица locations:**
- `id` - Primary Key
- `name` - Название локации
- `server` - IP адрес SIP сервера (inet)
- `subnet` - Подсеть телефонов (cidr)
- `voip_vlan` - VLAN для VoIP
- `vlan` - VLAN для LAN

**Таблица devices:**
- `mac` - MAC адрес (macaddr) - Primary Key
- `device_model` - Модель устройства (Yealink T27G, T23G, Fanvil, Cisco)

**Таблица profiles:**
- `id` - Primary Key
- `name` - ФИО сотрудника
- `email` - Email
- `device` - MAC адрес телефона (macaddr)
- `location_id` - FK на locations
- `internal_number` - Внутренний номер (уникальный)
- `external_number` - Внешний номер
- `ring_group` - Группа входящих
- `pickup_group` - Группа перехвата
- `is_active` - Активность

### Репозиторий (чистая архитектура)

Репозиторий **не предоставляет GetDB()**, все операции через методы:

```go
// Базовые CRUD
repos.Save(&profile)           // Создание/обновление
repos.Delete(&profile)         // Удаление
repos.FindAll(&profiles)       // Все записи
repos.FindByID(&profile, id)   // По ID
repos.FindOne(&device, "mac = ?", mac)  // По условию

// Специальные методы с JOIN
repos.FindProfilesWithLocations(isActive)  // Профили + локации через LEFT JOIN
repos.Exec(sql)                // Raw SQL (только для seed/migrations)
```

### Database Seeder

Seed создает тестовые данные:
- 6 локаций (Zags, Sov, Nad3, Ubil1, Ind4, Len15v)
- 10 устройств с MAC адресами
- 10 профилей сотрудников

```bash
cd backend
make seed
```

## Генератор конфигов Asterisk

Генератор работает с данными из PostgreSQL и создает все необходимые конфиги:

```bash
cd backend
make generator
```

Результаты сохраняются в `backend/results/`:

### Сгенерированные файлы

**tftpboot/** - конфиги для автопровижининга телефонов:
- `805ec0b4427c.cfg` - конфиг для Yealink T23G
- `805ec018abac.cfg` - конфиг для Yealink T27G
- И т.д. (по MAC адресу)

**UsersConf/** - SIP конфигурации пользователей:
- `User1119.conf` - SIP peer для внутреннего 1119
- `User1058.conf` - SIP peer для внутреннего 1058
- И т.д.

**ExtConf/** - файлы диалплана Asterisk:
- `ExtensionsCID.conf` - CallerID переменные
- `ExtensionsRG.conf` - Ring groups
- `ExtensionsRGCFG.conf` - Конфигурация ring groups
- `ExtensionsVMCFG.conf` - Голосовое меню и факс
- `ExtensionsOut.conf` - Исходящие правила
- `ExtensionsDP.conf` - Dialplan контексты
- `ExtensionsTrunkZags.conf` - Trunk для ЗАГС
- `ExtensionsTrankAdm.conf` - Trunk для администрации

**CiscoConf.txt** - конфигурация dial-peer для Cisco роутера

## Локальная разработка

1. **Запустите PostgreSQL:**
```bash
docker compose up -d postgres
```

2. **Заполните базу:**
```bash
cd backend
make seed
```

3. **Запустите API:**
```bash
make run
# или
go run main.go routes.go
```

4. **Запустите генератор:**
```bash
make generator
```

## Docker

### Healthchecks

PostgreSQL использует healthcheck для корректного запуска:
```yaml
healthcheck:
  test: ["CMD-READY", "pg_isready"]
  interval: 10s
  timeout: 5s
  retries: 5
```

Backend стартует только после готовности PostgreSQL:
```yaml
depends_on:
  postgres:
    condition: service_healthy
```

### Multi-stage build

Backend собирается через multi-stage Docker build для минимального размера образа:
- Builder stage: Go 1.24 Alpine
- Runtime stage: Alpine с ca-certificates

## Переменные окружения

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `DB_HOST` | Хост PostgreSQL | `localhost` |
| `DB_PORT` | Порт PostgreSQL | `5432` |
| `DB_USER` | Пользователь БД | `postgres` |
| `DB_PASSWORD` | Пароль БД | `postgres` |
| `DB_NAME` | Имя базы данных | `asterisk_manager` |
| `APP_PORT` | Порт API сервера | `8080` |
| `APP_VERSION` | Версия приложения | `dev` |

## Миграции

Миграции выполняются автоматически при старте приложения:

```go
repos := repositories.InitRepos(connStr)
repos.MigrateDB()  // Создает schema + таблицы + индексы
```

## Статистика генерации

После генерации выводится статистика:

```
📊 Статистика:
  Всего записей:        10
  Активных:             10
  Телефонов T27G:       7
  Телефонов T23G:       1
  Телефонов Fanvil:     0
  Cisco/Fax устройств:  2
  С MAC адресами:       9
  Только локальные:     0
```

## Разработка

### Тестирование
```bash
cd backend
go test ./...
```

### Линтинг
```bash
go vet ./...
```

### Сборка бинарника
```bash
cd backend
CGO_ENABLED=0 go build -o bin/asterisk-api main.go routes.go
```

## Технические детали

### Архитектурные решения

- ✅ **Нет GetDB()** - репозиторий скрывает детали работы с БД
- ✅ **Нет Preload** - используются явные JOIN запросы
- ✅ **Нет foreign key в моделях** - связи управляются через repository
- ✅ **DTO для JOIN** - ProfileWithLocation для joined данных
- ✅ **Централизованная обработка ошибок** - через Fiber ErrorHandler
- ✅ **camelCase в JSON** - единый стиль API

### Типы PostgreSQL

Использованы специфичные типы PostgreSQL:
- `inet` - IP адреса (10.16.0.102)
- `cidr` - Сети (10.1.191.0/26)
- `macaddr` - MAC адреса (80:5e:c0:b4:42:7c)

## Лицензия

MIT

## Автор

EgoriusRus - [GitHub](https://github.com/EgoriusRus)

## Поддержка

При возникновении проблем создайте [issue](https://github.com/EgoriusRus/Ghosterino-Asterisk-Manager/issues) в репозитории.
